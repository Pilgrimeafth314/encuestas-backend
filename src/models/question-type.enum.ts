/**
 * Can add new questions type here
 * answers save id of option or response, that is the same
 * can add textarea question (respond freely) soon?
 */
export enum QuestionType {
  Checkbox,
  MultipleOptions,
  OptionsList,
}
